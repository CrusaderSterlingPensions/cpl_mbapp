import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const DataRecapture = () => {
  return (
    <View style={styles.container}>
      <Text>DataRecapture</Text>
    </View>
  );
};

export default DataRecapture;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
  },
});
